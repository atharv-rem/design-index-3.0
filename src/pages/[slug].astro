---
export const prerender = false;

const toolId = Astro.url.searchParams.get("id");
const slug = Astro.params.slug;

if (toolId && slug) {
  return Astro.redirect(`/${toolId}/${slug}`, 301);
}

return Astro.redirect("/tools", 301);
---
